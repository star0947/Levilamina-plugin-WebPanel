name: Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    strategy:
      matrix:
        target_type: [server]
    steps:
      - uses: actions/checkout@v4

      - uses: xmake-io/github-action-setup-xmake@v1

      - name: Restore cached xmake dependencies
        uses: actions/cache@v6
        with:
          path: ~/.xmake
          key: xmake-${{ hashFiles('xmake.lua') }}

      - name: Build
        run: |
          xmake f -a x64 -m release -p windows --target_type=${{ matrix.target_type }} -y
          xmake

      - name: Prepare artifact
        run: |
          # 模组包已由 modpacker 生成在 bin/WebPanel/ 下
          New-Item -ItemType Directory -Force -Path artifact
          Copy-Item -Recurse -Force bin/WebPanel/* artifact/
          # 确保前端文件也进入 data/web（after_build 已尝试复制，这里作为双保险）
          if (Test-Path assets/web) {
            New-Item -ItemType Directory -Force -Path artifact/data/web
            Copy-Item -Recurse -Force assets/web/* artifact/data/web/
          }

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: WebPanel-${{ matrix.target_type }}
          path: artifact/